# DEPRECATED: This static secret file is replaced by External Secrets Operator
# The database-credentials secret is now dynamically created from cloud secret stores
# See: infrastructure/k8s/external-secrets/external-secrets.yaml

# This file is kept for reference and backward compatibility
# Remove this file once External Secrets Operator is fully deployed

apiVersion: v1
kind: Secret
metadata:
  name: database-credentials-fallback
  namespace: proj-app
  labels:
    app: database
    deprecated: "true"
  annotations:
    description: "Fallback secret - will be replaced by External Secrets Operator"
type: Opaque
data:
  username: ZGJhZG1pbg==  # dbadmin (base64)
  password: V0NtRUx5NnlLaTVlV2FNdQ==   # WCmELy6yKi5eWaMu (base64)
  database: YXBwZGI=      # appdb (base64)
